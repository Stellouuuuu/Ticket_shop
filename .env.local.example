# Surcharge locale optionnelle — uniquement si AlwaysData bloque la connexion directe
# Solution A (souvent suffisante) : ne pas créer ce fichier, .env suffit
# Solution B — tunnel SSH (npm run tunnel) puis :
# DB_HOST=127.0.0.1
# DB_PORT=3307
